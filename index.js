//Used from class demo
'use strict';

const express = require('express');
const app = express();

const minimist = require('minimist');

console.log('This is my working server. It works super well because I am so smart.');

app.listen(3000);