//Exports a constructor function.

//Has a prototype method called execute() that executes the correct operation given the above object.

//Add won't be the only action used.

//Has a prototype method called add() that will create an object representing a note, with an ID and the note as text properties, and console log the text of the note when the add command is executed.